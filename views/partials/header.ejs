<!DOCTYPE html>
<html lang="no">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Opplæringskontoret</title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>

    <nav class="navbar">
        <a href="/" class="logo">Opplæringskontoret</a>
        <div class="nav-links">
            <% if (typeof user !== "undefined" && user && user.role === "admin") { %>
                <a href="/">Oversikt</a>
                <a href="/bedrift/create">Opprett bedrift</a>
                <a href="/bedrift/register-elev">Registrer elev</a>
                <a href="/bedrift/send-vurdering">Send vurdering</a>
                <a href="/logout">Logg ut</a>
            <% } else if (typeof user !== "undefined" && user && user.role === "bedrift") { %>
                <a href="/bedrift/dashboard">Bedriftoversikt</a>
                <a href="/logout">Logg ut</a>
            <% } else if (typeof user !== "undefined" && user && user.role === "elev") { %>
                <a href="/student/dashboard">Elevoversikt</a>
                <a href="/logout">Logg ut</a>
            <% } else { %>
                <a href="/login-user">Logg inn</a>
                <a href="/register">Registrer</a>
            <% } %>
        </div>
    </nav>

    <div class="container">